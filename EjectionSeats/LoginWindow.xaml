<Window x:Class="EjectionSeats.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="250" Width="350" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Логин -->
        <TextBlock Text="Логин:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="txtLogin" Grid.Row="1" Height="25" Margin="0,0,0,10"/>

        <!-- Пароль -->
        <TextBlock Text="Пароль:" Grid.Row="2" Margin="0,0,0,5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="3" Height="25" Margin="0,0,0,15"/>

        <!-- Кнопка -->
        <Button x:Name="btnLogin" Content="Войти" Grid.Row="4" Height="30" Width="100"
                HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,5,0,0"
                Click="BtnLogin_Click"/>

        <!-- Сообщение об ошибке -->
        <TextBlock x:Name="txtError" Grid.Row="4" Foreground="Red" TextWrapping="Wrap"
                   VerticalAlignment="Bottom" Margin="0,0,0,5"/>
    </Grid>
</Window>